{
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "name": "smart-trafik-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "preview": "vite preview",
    "build": "vite build",
    "dev": "vite",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "hls.js": "^1.7.1"
  }
}
